@{
    ViewBag.Title = "个人房费报表";
    Layout = "~/Views/Shared/_LayoutIndex.cshtml";
}
<link href="~/Content/Scripts/jquery.monthpicker/jquery.monthpicker.css" rel="stylesheet" />
<script src="~/Content/Scripts/jquery.monthpicker/jquery.monthpicker.js"></script>
<style>
    .ui-jqgrid tr.jqgrow td {
        white-space: normal !important;
        height: auto;
        vertical-align: text-top;
        padding-top: 2px;
    }
</style>
<script type="text/javascript">
    $(document).ready(function () {
        $('#txtInputDate').monthpicker({
            years: [2017, 2018, 2019, 2020, 2021, 2022],
            topOffset: 6
        })
        GetGrid();

        //绑定键盘按下事件
        $(document).keypress(function (e) {
            //回车键事件
            if (e.which == 13) {
                $("#keywords").focus();
                $("#keywords").select();
                $("#btnSearch").click();
            }
        });
    });

    var CompanyId = "", DepartmentId = "", DepartmentName = "";


    Date.prototype.pattern = function (fmt) {
        var o = {
            "M+": this.getMonth() + 1, //月份
            "d+": this.getDate(), //日
            "h+": this.getHours() % 12 == 0 ? 12 : this.getHours() % 12, //小时
            "H+": this.getHours(), //小时
            "m+": this.getMinutes(), //分
            "s+": this.getSeconds(), //秒
            "q+": Math.floor((this.getMonth() + 3) / 3), //季度
            "S": this.getMilliseconds() //毫秒
        };
        var week = {
            "0": "/u65e5",
            "1": "/u4e00",
            "2": "/u4e8c",
            "3": "/u4e09",
            "4": "/u56db",
            "5": "/u4e94",
            "6": "/u516d"
        };
        if (/(y+)/.test(fmt)) {
            fmt = fmt.replace(RegExp.$1, (this.getFullYear() + "").substr(4 - RegExp.$1.length));
        }
        if (/(E+)/.test(fmt)) {
            fmt = fmt.replace(RegExp.$1, ((RegExp.$1.length > 1) ? (RegExp.$1.length > 2 ? "/u661f/u671f" : "/u5468") : "") + week[this.getDay() + ""]);
        }
        for (var k in o) {
            if (new RegExp("(" + k + ")").test(fmt)) {
                fmt = fmt.replace(RegExp.$1, (RegExp.$1.length == 1) ? (o[k]) : (("00" + o[k]).substr(("" + o[k]).length)));
            }
        }
        return fmt;
    }

    //搜索
    function btn_Search() {
        var keywords = $("#txtInputDate").val();

        

        $("#gridTable").jqGrid('setGridParam', {
            url: "@Url.Content("~/DormModule/RoomCost/GetRoomCostJson")?keywords=" + escape(keywords), page: 1
        }).trigger('reloadGrid');
    }

    function btn_Excel() {
        var keywords = $("#txtInputDate").val();


        if (keywords == undefined || keywords == "" || keywords == 'null' || keywords == 'undefined') {
            alert("请选择计费日期");
            return false;
        }
        

        window.open(encodeURI("RoomCostExcelExport?keywords=" + keywords));

    }

    //加载表格
    function GetGrid() {
        //var startDate = $("#txtStartDate").val();
        //var endDate = $("#txtEndDate").val();
        //if (startDate == undefined || startDate == "" || startDate == 'null' || startDate == 'undefined')
        //{
        //    alert("请填写开始日期");
        //    return false;
        //}
        //if (endDate == undefined || endDate == "" || endDate == 'null' || endDate == 'undefined')
        //{
        //    alert("请填写结束日期");
        //    return false;
        //}

        var SelectRowIndx;
        $("#gridTable").jqGrid({
            url: "@Url.Content("~/DormModule/RoomCost/GetRoomCostJson")",
            datatype: "json",
            height: $(window).height() - 178,
            autowidth: true,
            colModel: [
                { label: '房间号', name: '房间号', index: '房间号', width: 100, align: 'left' },
                { label: '房管', name: '房管', index: '房管', width: 100, align: 'left' },
                { label: '工号', name: '工号', index: '工号', width: 120, align: 'left' },
                { label: '姓名', name: '姓名', index: '姓名', width: 120, align: 'left' },
                { label: '住房类型', name: '住房类型', index: '住房类型', width: 100, align: 'left' },
                { label: '住房面积', name: '住房面积', index: '住房面积', width: 80, align: 'left' },
                { label: '住房代扣', name: '住房代扣', index: '住房代扣', width: 80, align: 'left' },
                { label: 'IPTV账单', name: 'IPTV账单', index: 'IPTV账单', width: 80, align: 'left' },
                { label: '闭路代扣', name: '闭路代扣', index: '闭路代扣', width: 80, align: 'left' },
                { label: '维修费', name: '维修费', index: '维修费', width: 80, align: 'left' },
                { label: '垃圾费', name: '垃圾费', index: '垃圾费', width: 80, align: 'left' },
                { label: '水吨数', name: '水吨数', index: '水吨数', width: 80, align: 'left' },
                { label: '水单价', name: '水单价', index: '水单价', width: 80, align: 'left' }, 
                { label: '水费分', name: '水费分', index: '水费分', width: 80, align: 'left' },
                { label: '电度数', name: '电度数', index: '电度数', width: 80, align: 'left' },
                { label: '电单价', name: '电单价', index: '电单价', width: 80, align: 'left' },
                { label: '电费分', name: '电费分', index: '电费分', width: 80, align: 'left' },
                { label: '其他代扣', name: '其他代扣', index: '其他代扣', width: 80, align: 'left' },
                { label: '物业小计', name: '物业小计', index: '物业小计', width: 80, align: 'left' },
                { label: '部门', name: '部门', index: '部门', width: 80, align: 'left' },
            ],
            viewrecords: true,
            rowNum: 10000,
            //rowList: [30, 50, 100, 500, 1000],
            //pager: "#gridPager",
            sortname: 'PersonCode',
            sortorder: 'desc',
            rownumbers: false,
            shrinkToFit: false,
            gridview: true,
            multiselect: false,
            //cellattr: addCellAttr,

            // useColSpanStyle:true,
            onSelectRow: function () {
                SelectRowIndx = GetJqGridRowIndx("#" + this.id);
            },
            gridComplete: function () {
                //LoadViewList();
                $("#" + this.id).jqGrid('setSelection', SelectRowIndx);
            }
        });
        columnModelData("#gridTable");
        //自应高度
        $(window).resize(function () {
            $("#gridTable").setGridHeight($(window).height() - 178);
        });

    }
</script>

<!--左边-->
<!--中间-->
<div>
    <div class="btnbartitle">
        <div>
            住宿费用报表 <span id="CenterTitle"></span>
        </div>
    </div>
    <!--工具栏-->
    <div class="tools_bar" style="border-top: none; margin-bottom: 0px;display:none">
        <div class="PartialButton">
            @Html.Partial("_PartialButton")
        </div>

    </div>
    <!--列表-->
    <div id="grid_List">
        <div class="bottomline QueryArea" style="margin: 1px; margin-top: 0px; margin-bottom: 0px">
            <table border="0" class="form-find" style="height: 45px;">
                <tr>
                    <th>
                        选择月份：
                    </th>
                    <td>
                        <input id="txtInputDate" type="text" class="txt" style="width: 200px" />
                    </td>
                    
                    <td>
                        <input id="btnSearch" type="button" class="btnSearch" value="搜 索" onclick="btn_Search()" />
                    </td>
                    <td>
                        <input id="btnExcel" type="button" class="btnSearch" value="导 出" onclick="btn_Excel()" />
                    </td>
                </tr>
            </table>
        </div>
        <table id="gridTable"></table>
        <div id="gridPager"></div>
    </div>
    <!--视图-->

</div>

